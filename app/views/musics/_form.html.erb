<%= form_with(model: music, local: true) do |form| %>
  <% if music.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(music.errors.count, "error") %> prohibited this music from being saved:</h2>

      <ul>
      <% music.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :music_image %>

    <%#= form.text_field :music_image %>
    <%= form.file_field :music_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
